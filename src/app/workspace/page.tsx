import { CrmWorkspace } from "@/components/crm-workspace";

export const metadata = {
  title: "Northstar CRM",
  description: "Interactive Dynamis Signal CRM workspace.",
};

export default function WorkspacePage() {
  return <CrmWorkspace />;
}
